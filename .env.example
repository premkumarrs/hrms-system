# Docker Compose variable substitution (repo root).
# Copy to .env and align DB_PASSWORD with your PostgreSQL volume.
# backend/.env is used inside containers via env_file.

DB_NAME=hrms_db
DB_USER=postgres
DB_PASSWORD=postgres
